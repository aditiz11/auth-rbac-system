package com.authentication.auth_system.entity;

public enum Role {
    // there are two type of user:  user and admin
    //I used enum to restrict roles to a fixed set of values instead of using plain strings.
    USER,
    ADMIN
}
